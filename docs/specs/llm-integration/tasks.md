# Список задач: LLM Integration

## Обзор

План работ по устранению проблемы рывкового стриминга ответа (Issue #74 follow-up).
Документ фиксирует прогресс анализа и реализации.

**Текущий статус:** Фаза 12 — Готово к финальному full functional suite

---

## CRITICAL RULES

- Не менять поведение контрактов `message.llm.reasoning.updated` / `message.llm.text.updated` без синхронизации со спецификациями `llm-integration` и `realtime-events`.
- Не удалять незавершённые пункты при актуализации плана.
- Не запускать полный `npm run test:functional` без отдельного подтверждения пользователя.

---

## Текущее состояние

### Выполнено
- ✅ Проверены актуальные ревью-замечания PR и синхронизация спецификаций/трассировки требований.
- ✅ Подтверждено, что equal-timestamp дедупликация для `message.llm.text.updated` в main/renderer уже исправлена и покрыта unit-тестами.
- ✅ Проведён повторный анализ end-to-end пути стриминга (`provider -> MainPipeline -> EventBus -> IPCChatTransport -> AgentChat`).
- ✅ Выявлен вероятный bottleneck на UI-слое: массовые ререндеры списка сообщений с тяжёлым markdown-рендерингом (`Streamdown`) при каждом snapshot update.

### В работе
- 🔄 Фаза 12: ожидание подтверждения на финальный полный `npm run test:functional`.

### Запланировано

#### Фаза 11: Streaming Smoothness Follow-up

- [ ] Шаг 1. Локализовать и зафиксировать целевой контракт оптимизации рендера:
  - [x] Зафиксировать, какие части дерева должны ререндериться только для изменённого сообщения.
  - [x] Зафиксировать критерий «без рывков» для сценария длинного текстового стриминга.

- [x] Шаг 2. Оптимизировать рендер сообщений в renderer:
  - [x] Добавить мемоизацию `AgentMessage` (`React.memo`) с корректным сравнением пропсов.
  - [x] Обеспечить стабильность пропсов, влияющих на список сообщений в `AgentChat`.
  - [x] Исключить лишние ререндеры неизменённых элементов списка при потоковых `message.updated`.

- [x] Шаг 3. Добавить регрессии на уровень unit/UI:
  - [x] Добавить тест(ы), подтверждающие отсутствие лишних ререндеров соседних сообщений при обновлении одного `llm` сообщения.
  - [x] Добавить/уточнить тест(ы) для непрерывной доставки `text-delta` без визуального burst-паттерна на уровне transport/render-инвариантов.

- [x] Шаг 4. Актуализировать спецификации:
  - [x] Обновить `docs/specs/llm-integration/design.md` (раздел runtime/rendering strategy и покрытие тестами).
  - [x] При необходимости обновить `docs/specs/realtime-events/design.md` (ограничения/гарантии для streaming UX на renderer-слое). Не потребовалось: контракты realtime-событий не менялись.

- [x] Шаг 5. Промежуточная валидация:
  - [x] Прогнать целевые unit-тесты по изменённым модулям.
  - [x] Прогнать `npm run validate`.
  - [x] При необходимости прогнать одиночный functional сценарий стриминга (`test:functional:single`).

- [ ] Шаг 6. Завершение:
  - [x] Подготовить итоговый отчёт по фактическому эффекту исправления.
  - [ ] Запросить подтверждение на финальный полный `npm run test:functional`.
